package com.example.leclubparvechin.model.user

data class User(val id: String,
                val username: String) {

    //Classe représentant un utilisateur de l'application (compte utilisateur)
}